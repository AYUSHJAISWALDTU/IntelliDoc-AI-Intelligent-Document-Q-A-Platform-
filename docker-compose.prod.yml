version: "3.9"

services:
  backend:
    environment:
      - APP_ENV=production
    command: >
      uvicorn app.main:app
      --host 0.0.0.0 --port 8000
      --log-level info

  frontend:
    environment:
      - VITE_API_URL=https://api.example.com
